# oasdiff err-ignore / warn-ignore file.
# One change description per line (case-insensitive substring match against oasdiff output).
# Used to acknowledge known Meraki API drift without re-vendoring the baseline spec.
# Example:
#   api the request property 'foo' became required for the status '200'
