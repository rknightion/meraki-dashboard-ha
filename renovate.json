{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "description": "Do not bump requires-python; the floor tracks Home Assistant's supported Python and is managed manually",
      "matchDepNames": ["python"],
      "enabled": false
    }
  ]
}
